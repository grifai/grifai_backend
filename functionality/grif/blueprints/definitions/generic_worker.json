{
  "id": "generic_worker",
  "name": "Generic Worker",
  "task_types": ["search", "execute", "remind", "generate", "operate", "research", "monitor", "coach", "compare"],
  "domains": ["general"],
  "description": "General-purpose fallback agent for tasks that don't match a specific blueprint.",
  "template": "generic_worker",
  "default_tools": ["web_search"],
  "default_model": "claude-sonnet-4-6",
  "typical_complexity": "simple",
  "plan_pattern": "pipeline",
  "typical_agents": [
    {"role": "worker", "order": 1}
  ],
  "required_fields": [],
  "optional_fields": []
}
